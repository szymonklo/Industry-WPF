<Page x:Class="Industry_WPF.Views.WorldView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Industry_WPF.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="WorldView">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="67*"/>
            <ColumnDefinition Width="67*"/>
            <ColumnDefinition Width="67*"/>
            <ColumnDefinition Width="32*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button x:Name="ShowFactories" Content="Factories" Grid.Column="0" Grid.Row="2"/>

        <TextBlock x:Name="RoundNumber" Grid.Column="3" Grid.Row="1"/>
        <Button x:Name="NextRound" Content="Next Round" Grid.Column="3" Grid.Row="2"/>

        <ContentControl Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" x:Name="ActiveItem"/>

    </Grid>
</Page>
